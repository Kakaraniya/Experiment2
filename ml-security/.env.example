# Copy values into your local environment; do not commit real secrets.
JWT_SECRET_KEY=replace-with-a-random-value-at-least-32-characters
JWT_EXPIRE_MINUTES=30
DEMO_USERNAME=student
# Optional: provide your own Argon2id password hash.
# DEMO_PASSWORD_HASH=$argon2id$...
